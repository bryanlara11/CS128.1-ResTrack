## Backend server
PORT=5000
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

## Postgres connection (local)
DB_HOST=localhost
DB_PORT=5432
DB_USER=*ENTER USERNAME*
DB_PASSWORD=*ENTER PASSWORD*
DB_NAME=restrack

## Postgres connection (hosted, preferred in production)
# DATABASE_URL=postgresql://user:password@host:5432/database
# DB_SSL=true

## JWT signing (required for login)
JWT_SECRET=*ENTER A LONG RANDOM SECRET*

## REMOVE .EXAMPLE FROM FILENAME AND FILL IN THE VALUES BEFORE RUNNING THE BACKEND SERVER. 

